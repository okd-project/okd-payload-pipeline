apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: base
  namespace: okd
spec:
  source:
    type: Git
    git:
      uri: 'https://github.com/vrutkovs/openshift-images'
      ref: centos9
    contextDir: base
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Dockerfile.centos9
      from:
        kind: "ImageStreamTag"
        name: "release:builder"
  output:
    to:
      kind: ImageStreamTag
      name: 'release:base'
