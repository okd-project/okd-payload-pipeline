apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: aws-ebs-csi-driver-operator
spec:
  source:
    type: Git
    git:
      uri: 'https://github.com/openshift/aws-ebs-csi-driver-operator'
      ref: release-4.12
    contextDir: .
    images: []
  strategy:
    type: Docker
    dockerStrategy:
      imageOptimizationPolicy: SkipLayers
      dockerfilePath: Dockerfile.rhel7
      from:
        kind: "ImageStreamTag"
        name: "release:base"
  output:
    to:
      kind: ImageStreamTag
      name: 'release:aws-ebs-csi-driver-operator'
