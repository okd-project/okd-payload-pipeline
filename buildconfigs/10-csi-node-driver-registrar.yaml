apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: csi-node-driver-registrar
spec:
  source:
    type: Git
    git:
      uri: 'https://github.com/openshift/csi-node-driver-registrar'
      ref: release-4.12
    contextDir: .
    images: []
  strategy:
    type: Docker
    dockerStrategy:
      imageOptimizationPolicy: SkipLayers
      dockerfilePath: Dockerfile.openshift
      from:
        kind: "ImageStreamTag"
        name: "release:base"
  output:
    to:
      kind: ImageStreamTag
      name: 'release:csi-node-driver-registrar'
